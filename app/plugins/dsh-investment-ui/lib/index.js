/**
 * Investment Auto 2.0 client UI — node half.
 *
 * The browser roster (`dsh.client` in package.json) serves lib/client.js as
 * this package's client entry; the host half is intentionally empty. The
 * client registers keyed tool cards into `tool.call.toolview`.
 */
export const name = "@investment-auto/dsh-investment-ui";
export const inject = [];
export function apply() {}
