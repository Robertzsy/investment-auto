{
  "steps": [
    {"id": "resolve", "action": "security.resolve", "required": true, "retries": 1},
    {"id": "snapshot", "action": "security.snapshot", "required": true, "retries": 1},
    {"id": "research", "action": "security.research", "required": false, "retries": 1},
    {"id": "quality", "action": "security.validate", "required": true, "retries": 0},
    {"id": "synthesis", "action": "security.synthesize", "required": false, "retries": 1},
    {"id": "report", "action": "report.security", "required": true, "retries": 0}
  ]
}
